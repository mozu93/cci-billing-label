# CCI請求書・ラベル発行システム 開発ルール

PyQt6 製の Windows デスクトップアプリ。社内利用。

## UIレイアウト（画面サイズの差への対応）

利用者の画面は、大きな外部モニタから小さなノートPCまで幅がある。
**最小環境で壊れないことを先に満たす。** 広い画面での見栄えは後。

### 前提環境

- 最小: 1366×768 のノートPC（タスクバーを除く有効高さ約728px）
- ウィンドウ初期サイズ: 幅1280px × 高さ728px 以内
- ダイアログ: 初期幅780px以内 / 初期高さ600px以内 / 最小幅780px以下
- ウィンドウに `setMinimumSize` を設定する場合は 700×500px 以下

### 列を並べる表・フォーム

- 伸縮する列は原則1つに絞り、他は固定幅にする。伸縮列が複数あると配分が
  予測できず、狭いときの挙動を検証できない
- **固定幅の合計 + 伸縮列の最小幅 + 余白(margin + spacing) ≤ 780px** を満たす。
  列を追加・変更するときは、この式を必ず再計算する
- ヘッダーと明細行を別々のレイアウトで組む場合、伸縮列には**同じ最小幅**を
  与える。`QLabel` と `QComboBox` では最小幅が違うため、幅が足りなくなると
  縮み方が食い違って列がズレる
- 可能なら `QGridLayout` など単一のレイアウトで列を管理し、ズレる余地をなくす

### 表示する内容

- 同じ情報を複数の列に出さない。列幅を浪費し、見切れの原因になる
- 伸縮列の最小幅は、**実データで最も長い文字列が収まる幅**にする。
  「だいたい」で決めない
- 文字が収まるかは `QFontMetrics.horizontalAdvance()` で測れる

### 縦方向

- 縦に長いコンテンツは `QScrollArea`（または相当）で縦スクロール可能にする

### 検証

- 幅を変えたテストを書く（例: 1000 / 850 / 780 / 700px）。
  ヘッダーと行の各列の x と width が一致することを検証する
- 目視だけで判断しない。狭い幅での崩れは実測で確認する

参考実装: `tests/test_counter_line_layout.py`、`tests/test_counter_item_combo_label.py`

## データアクセス

- UI層に生のORMクエリと生SQLを書かない。`app/services/` のサービス関数を通す
- サービス関数は第1引数に `session: Session` を取る
- `get_session()` はUIに残してよい。トランザクション境界をUIが決める設計は変えない
- 業務ルールをサービス層へ移すときは、先にサービス層のテストを書く

## データの保存先

実行時データはリポジトリではなくユーザープロファイル配下に置く。
EXE配布時にインストール先へ書き込めないため。

- 設定・DB・ログ・トークン: `~/.cci-billing-label/`
- PDF出力・バックアップ: `~/cci-billing-label/`

派生元の `cci-billing` と共有していた名残があるため、
`app_config.migrate_legacy_data()` が起動時に一度だけ引き継ぐ。
移行済みの判定には `config.json` の有無を使う。**ディレクトリの存在では
判定できない**（`applog` がトップレベルの `get_logger()` から
`CONFIG_DIR.mkdir()` するため、設定を読む前に空のディレクトリができる）。

## テスト

- `python -m pytest tests/ -q` で全件実行
- **全件実行は数%の確率で `0xC0000409` により異常終了する既存不具合がある**
  （`test_main_window_nav.py` 付近、原因未特定）。終了コードがこれなら
  自分の変更が原因と早合点せず、再実行して判断する
- UIウィジェットのテストは `qtbot` と `memory_db` フィクスチャを使う
- 一時ファイルはリポジトリ内の `.tmp/` に置く
