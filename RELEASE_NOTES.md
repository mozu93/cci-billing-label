# CCI請求書・ラベル発行システム v2.5.4

2026年9月26日

## 新機能

- 領収書の窓口発行に「簡易インボイス」モードを追加しました。宛先入力なしで
  指定枚数ぶん連番発行できます（印刷専用、税区分を明細行ごとに選択可）。
- 名簿・入金管理・請求書/領収書を発行する名簿の3画面で、フリガナ・所属役職・
  会員番号・メールなど、表示中の全行が空欄の列を自動で隠すようにしました。
- 修正・再発行と入金管理に「業務名」の列を追加し、件名と分けて表示するように
  しました（単発発行の件名には目印として「※単発発行」を表示）。
- メールに署名を自動で付けられるようにしました（メール送信設定）。端末ごとに
  別ファイルで保存するため、config.jsonを他端末にコピーしても引き継がれません。

## 改善

- 名簿の必須項目を「事業所名のみ」に統一しました（代表者名は任意）。
- 名簿取り込みの重複判定を、事業所名・会員番号・氏名のいずれか一致で重複と
  みなす方式に変更しました（従来は事業所名＋氏名の両方一致が条件）。
- 単発発行の支払期限をデータベースに保存し、入金管理・督促メール対象の抽出に
  反映されるようにしました。あわせて入金管理の「種別」の絞り込みを廃止し、
  常に請求書のみを対象にしました。
- 起動時に、表示していないタブまで即座に生成していたのをやめ、開いたときに
  作るようにしました（起動時間を体感で大きく短縮）。
- 名簿・入金管理・発行画面のタブ切り替えで、名簿や会員の件数に応じてDB問い
  合わせが増えていた箇所をまとめて1回で取得するようにしました。
- まとめて発行のタブ見出しを「名簿・請求内容登録」に変更しました。

## 修正

- 事業所名と代表者名が同じ（屋号のない個人事業主）場合に、宛名ラベルで
  氏名が二重に表示されたり、個人名に「御中」が付いてしまう問題を修正しました。
- まとめて発行の名簿エリアで、幅のずれと二重の余白により無駄な空白ができて
  いた問題を修正しました。
- 発行設定・発行項目がウィンドウ幅より広いとき、内容が見切れて元に戻す方法が
  なかった問題を修正しました（横スクロールを有効化）。
- 簡易インボイスの宛先欄が入力できてしまい紛らわしかったため、使用不可に
  グレーアウトしました。
- まとめて発行の発行画面・宛名ラベル画面で、行選択時に行全体の色が変わらな
  かった問題を修正しました。

# CCI請求書・ラベル発行システム v2.5.3

2026年9月25日

## ご確認ください（まとめて発行の操作が変わります）

- **「発行する」を押すと、発行方法と支払期日を確認する画面が出るようになりました。**
  発行方法と支払期日（領収書は発行日）は「発行の設定」から外し、発行のたびにここで
  選びます。支払期日の初期値は、その名簿で前回使った支払期日です。
- **メール送付は、確認1回の一括送信になりました。** 1件目を見本に送信確認画面が
  1回だけ開き、ここで直した件名・本文は宛名などを1件ずつ差し込んで全件に使われます。
  「◯件に送信します」の確認のあと、進み具合を表示しながら送ります（中止できます）。
  66件なら約2分かかります。
- **メールを送らなかった書類は「準備中」に戻るようになりました。** 確認でキャンセルした、
  途中で中止した、送信に失敗した、メールアドレスがない、のいずれかの場合です。
  番号はそのままで、次に発行するときに使われます。以前は、キャンセルしても
  発行済みのまま残っていました。
- **請求書・領収書を発行済みの名簿の行は、削除できなくなりました。** 参加をやめた
  場合は「参加キャンセル」を使ってください。

## 新機能

- テスト送信モードを追加しました（メール送信設定）。オンの間は、アプリが送る
  すべてのメールがテスト送信先だけに届きます。画面下に赤い表示が出ます。
- 名簿を Excel に出力できるようにしました（名簿の見出しの行の「Excel出力」）。
  上のダッシュボードの CSV・Excel 出力は廃止しました。
- まとめて発行の名簿の表に「所属・役職」の列を追加しました。

## 改善

- 所属・役職を、どの画面でも氏名（代表者名）の左に並べました。
- 名簿・請求内容の上のエリアに「ダッシュボード（発行・入金の状況）」、下の名簿に
  「名簿：業務名　件名」の見出しを付けました。
- 名簿のボタン名を短くし、見切れないようにしました。
  「他名簿から追加」（他の名簿からコピー）は廃止しました。
- プルダウンの候補一覧を、入力欄の下に開く見やすい一覧にしました。
- ダッシュボードの件名の列が、幅によってつぶれて読めなかった問題を修正しました。

## 修正

- 名簿・請求内容を作成してExcelで取り込んだあと、名簿がその場で表示されなかった
  問題を修正しました。
- プルダウンの候補が切れて見えなかった問題を修正しました。

# CCI請求書・ラベル発行システム v2.5.2

2026年9月25日

## ご確認ください

- **2027年4月から、請求書・領収書の番号の形が変わります。**
  2027年3月までは「INV-202703-0012」のように年月と月ごとの連番、
  2027年4月以降は「INV-2027-0001」のように**年度と年度内の通し番号**になります
  （毎年4月に1から数え直し）。すでに発行した番号は変わりません。
  **番号は全端末で共有するため、2027年3月末までにすべての端末をこのバージョン以降に
  更新してください。** 古いバージョンの端末は、4月以降も今の形で番号を付けてしまいます。
- **年度は4月〜翌3月で扱うようになりました。** 名簿・請求内容、請求書を発行・領収書を
  発行、入金管理、修正・再発行の年度は、どれも当年度を初期表示します
  （例：2027年2月に開くと2026年度）。
- **まとめて発行の「完了」「完了を戻す」「年度更新」を廃止しました。** 名簿は年度ごとに
  新しく作り、年度で絞り込んで表示します。以前「完了」にした名簿も、そのまま
  表示されます（データは変わりません）。請求項目テンプレートは年度をまたいで残ります。

## 改善

- 入金管理に「年度」の絞り込みを追加しました。前年度以前に未入金が残っていると
  お知らせを表示します。
- まとめて発行（請求書・領収書を発行）の画面を整理しました。発行方法・支払期日・
  発行元・PDF出力などは「発行の設定」にまとめ、普段は1行の要約で表示し、「変更」で
  編集します。検索とExcelは一覧のすぐ上、プレビューと発行のボタンは下部の1行に
  並べました。見切れていた項目も読めるようになりました。
- 名簿・請求内容のCSV出力・Excel出力を、年度の行の右端に移しました。

## 修正

- 修正・再発行で年度を選ぶと、単発発行の書類が見つからないことがあった問題を
  修正しました。単発発行の書類は発行日で年度を判定します。

# CCI請求書・ラベル発行システム v2.5.1

2026年9月25日

## ご確認ください（単発発行の動きが変わります）

- **発行後も入力内容が残るようになりました。** 続けて同じような書類を作れます。
  新しく入力し直すときは、画面下の「すべてクリア」を押してください。
  何も変えずにもう一度「発行する」を押すと、二重発行の確認が出ます。
- **印刷もメール送信もしなかった発行は、自動で取り消されます。** 保存先の選択や
  メールの送信をキャンセルした場合です。取り消した番号は欠番になり、操作ログに
  「発行取消」が残ります（「修正・再発行」は対象外で、元の書類は消えません）。
- **領収書の消費税額を、請求書と同じ「切り捨て」にそろえました。** これまで領収書は
  1円多くなることがありました（例：10,000円で請求書909円・領収書910円）。
  過去の領収書を再発行すると、税額が1円下がって表示される場合があります。

## 新機能

- 単発発行に「プレビュー」ボタンを追加しました。番号を使わずに、「見本」の
  透かし入りPDFで仕上がりを確認できます。
- 単発発行に「テスト送信」ボタンを追加しました（発行方法が「メール送付」のとき）。
  番号を使わずに、自分宛てに【テスト】付きの請求書メールと見本PDFを送れます。
- メールを送らなかったとき（キャンセル・送信失敗など）に、同じ番号のまま
  印刷に切り替えられるようにしました。
- 単発発行の数量に0を入力できるようにしました。数量0の行は書類に印字されません。

## 改善

- 単発発行の画面配置を見直しました。住所と「宛名に役職・氏名を印字する」
  「窓あき封筒で住所を印字する」を宛先欄に移し、支払期日は合計の横に表示します。
- プルダウン・数値欄・日付欄が、マウスホイールで変わらないようにしました
  （画面をスクロールしようとして値が変わる誤入力を防ぐため）。
- 請求書PDFの窓あき封筒用の郵便番号・住所の文字を大きくしました（9pt→11pt）。
- 会員マスタのCSVインポートで、取り込み先ごとの設定状況を表示し、設定済みの
  行を色分けしました。同じ取り込み先を複数の列に設定したままでは実行できません。

## 修正

- 単発発行で会員番号を1桁入力すると、それ以上入力できなかった問題を修正しました。
  候補は↓キーとEnterでも選べます。
- 発行項目から「テンプレ登録」したとき、行の単位が引き継がれなかった問題を
  修正しました。
- 会員マスタの一覧で、事業所名の列幅を変えられなかった問題を修正しました。
- 起動直後にアプリを閉じたとき、上長への発行通知メールを送ったあとにアプリを
  閉じたとき、更新ファイルのダウンロード中にアプリを閉じたときに、
  異常終了することがあった問題を修正しました。

# CCI請求書・ラベル発行システム v2.5.0

2026年9月23日

## ご確認ください（データの保存場所が変わります）

設定・データベース・ログの保存先が `.cci-billing` から `.cci-billing-label` へ変わりました。
**更新後の初回起動時に、これまでのデータは自動で引き継がれます。操作は不要です。**

設定ファイル（config.json）を端末間でコピーして共有している場合、
配布先が `ユーザーフォルダ\.cci-billing-label\config.json` に変わります。

これまでに出力したPDFとバックアップは、従来のフォルダ（`ユーザーフォルダ\cci-billing`）に
残ります。新しく出力するぶんは `cci-billing-label` フォルダに保存されます。

## 改善・修正

- 単発発行・修正再発行の発行項目に「単位」欄を追加しました。これまで単位は
  テンプレートの値で固定され、画面から変更できませんでした。
- 発行項目の見出しと入力欄がずれて、単価が「数量」欄のように見えていた問題を
  修正しました。ウィンドウ幅が狭いときに発生していました。
- 発行項目の「項目」欄で、品目名が見切れて読めないことがある問題を修正しました。
- 請求書メール送信の確認画面が、画面の小さいノートPCで下部に収まらず、
  送信ボタンを押せないことがある問題を修正しました。
- 名簿一覧の「請求書発行済」が、領収書も発行済みの会員を数えていなかった問題を
  修正しました。実際より少ない件数が表示されていました。
- 名簿一覧の請求総額に領収書が二重で計上されていた問題を修正しました。
- 入金管理・宛名ラベル発行で、アプリを起動したあとに作成・再開した名簿が
  一覧に出てこない問題を修正しました。
- アプリを閉じたときに異常終了していた問題を修正しました。

# CCI請求書・ラベル発行システム v2.4.8

2026年9月11日

## 改善・修正

- まとめて発行の案件設定で、既存の明細名を編集して別の明細名へ変更した場合も保存できるようにしました。
- 請求書発行済みの事業所について、名簿の宛先情報を修正して再発行した際に、修正内容が請求書へ反映されるようにしました。
- 発行済み請求書の数量・単価を再発行時に更新できるようにしました（入金済みを除く）。
# CCI請求書・ラベル発行システム v2.4.7

2026年9月11日

## 改善・修正

- 請求書の発行元情報が長くて折り返す場合、行に収まるよう文字サイズを自動調整するようにしました。

# CCI請求書・ラベル発行システム v2.4.6

2026年9月11日

## 改善・修正

- まとめて発行の請求書・領収書で、発行前に選択行のPDFを確認できる「選択した書類をプレビュー」機能を追加しました。
- プレビューでは数量・単価・発行元・口座・印鑑など現在の設定を反映し、発行番号の採番や発行済み状態への変更は行いません。
- まとめて発行画面の発行元・口座・印鑑の表示が画面幅によって切れる問題を修正しました。

# CCI請求書・ラベル発行システム v2.4.5

2026年9月11日

## 修正・改善

- まとめて発行の数量・単価を変更した時点で共有DBへ保存し、他ユーザーが再表示した際にも反映されるようにしました。
- まとめて発行の発行元・口座・印鑑の変更を案件設定へ即時保存するようにしました。
- 複数ユーザーによる同時編集時の保存競合、保存失敗時の表示、名簿削除時の関連データ削除に対応しました。

# CCI請求書・ラベル発行システム v2.4.4

2026年9月11日

## 改善・修正

- まとめて発行の**案件設定で、プラン（品目）ごとのデフォルト数量に0を設定できる**ようにしました。使わないプランを企業ごとに手動で0にする手間を減らせます。
- 起動時にウィンドウが画面より大きく表示され、動かせなくなることがある不具合を修正しました（マルチモニタ・拡大率混在環境でのDPI対応を強化）。
- アプリのアイコン生成スクリプトの不具合を修正し、タスクバー等に表示されるアイコンをより高解像度なものにしました。

# CCI請求書・ラベル発行システム v2.4.3

2026年8月21日

## 改善・修正

- **まとめて発行の名簿エリアを広く表示**するようにしました。案件一覧との境界を上下にドラッグして、作業に合わせて高さを調整できます。
- 名簿に**検索欄**を追加しました。事業所名・フリガナ・氏名・氏名フリガナを入力すると、該当する行だけをリアルタイムに表示します。
- 左メニューの**入金管理アイコンを￥マーク**に変更しました。

# CCI請求書・ラベル発行システム v2.4.2

2026年8月19日

## パッチ更新

- メール配信結果確認機能を含む v2.4.x 系の安定性を維持するためのパッチリリースです。
- 配信状況の更新、失敗理由の表示、設定情報の取り扱いを継続して利用できます。

# CCI請求書・ラベル発行システム v2.4.1

2026年8月19日

## メール配信結果の確認

- Microsoft 365の送信受付結果と、相手先への実際の配信結果を分けて確認できるようにしました。
- 再発行画面の「配信状況を更新」で、配信済み・確認待ち・配信失敗・隔離などを確認できます。
- 配信失敗時は、Microsoft 365のメッセージ追跡情報から失敗理由を表示します。
- 配信状況の確認には、Entra IDアプリケーション権限 `ExchangeMessageTrace.Read.All` と管理者同意、追跡用クライアントシークレットが必要です。
- 送信日時の日本時間・UTC変換に対応しました。

# CCI請求書・ラベル発行システム v2.4.0

2026年8月17日

## 主な変更

- **入金管理を左メニューの独立項目へ移動**しました。単発発行・まとめて発行のどちらの請求書も、同じ画面で入金記録・支払済み更新を行えます。
- 入金管理を開くたびに一覧を更新するようにし、ほかの画面で行った発行や入金修正を再起動せず確認できます。
- まとめて発行には、名簿・請求内容、請求書発行、領収書発行だけを表示する構成へ整理しました。

# CCI請求書・ラベル発行システム v2.3.1

2026年8月17日

## 修正

- まとめて発行内のプルダウンは、マウスを載せただけでホイールを回しても選択値が変わらないようにしました。候補を開いた後は、従来どおりホイールで候補一覧をスクロールできます。

# CCI請求書・ラベル発行システム v2.3.0

2026年8月17日

## 主な変更

- まとめて発行の名簿に**「キャンセル」列**を追加しました。発行済み後の参加キャンセルにも対応し、請求書・領収書の履歴を残したまま、今後の発行対象と入金管理から除外できます。キャンセルの取り消しも可能です。
- 名簿・請求内容の集計では、キャンセル済みの参加者を「全件」「請求書発行済」「領収書発行済」の対象外とします。
- まとめて発行の一覧に**「入金件数」**を追加し、入金額とともに実際の入金記録を表示するようにしました。
- アプリの更新配布先を、ソースコードとは分離した公開配布リポジトリへ移行します。既存版にも更新通知が届くよう、今回の v2.3.0 だけは従来の配布先にも公開します。更新後のアプリは新しい公開配布先から以後の更新を確認します。

# CCI請求書・ラベル発行システム v2.2.7

2026年8月10日

## 主な変更（まとめて発行 → 名簿・請求内容）

- 一度確定した名簿にも、**あとから行を追加で取り込める**ことを分かりやすくしました。キャンセルや追加申込で名簿が増えるときに使えます。
  - 名簿の「取り込み（Excel/貼り付け）」ボタンを**「追加取り込み（Excel/貼り付け）」**に変更しました。
  - 取り込み画面の先頭に「現在の名簿：〇件／取り込んだデータは、いまの名簿を消さずに追加されます」と表示するようにしました。既存の行が消えないことを取り込む前に確認できます。
  - 取り込みボタンも「名簿に追加する」に変わり、実行前に「現在の名簿〇件に〇件を追加します」という確認を挟むようにしました。
- **重複チェック**を追加しました。すでに名簿にある行（会員番号が同じ、または事業所名＋氏名が同じ）は既定で取り込まないようにしています。
  - 貼り付けたデータの中で重複している行も1件だけ取り込みます。
  - プレビューの先頭に「取込」列を設け、行ごとに「追加」「重複（スキップ）」が分かるようにしました。
  - 同じ事業所をあえて2行登録したい場合は、「すでに名簿にある行は取り込まない」のチェックを外してください。
- 名簿の件数が変わったとき、上の一覧の「全件」「未発行」などが**その場で更新される**ようにしました（選択や表示位置はそのまま保たれます）。

# CCI請求書・ラベル発行システム v2.2.6

2026年8月7日

## 主な変更

- 請求書PDFの税率別内訳の税額欄を「10%税額」「8%税額」から**「うち10%税額」「うち8%税額」**へ変更しました。対象小計に含まれる税額であることがわかりやすくなります。
- 領収書PDFの内訳も同様に、税率ごとに**「うち10%税額」「うち8%税額」**と表示するようにしました（従来はどちらも「うち消費税」でした）。

# CCI請求書・ラベル発行システム v2.2.5

2026年8月4日

## 主な変更（画面デザインの刷新）

- 「わかりにくい」というご指摘を受け、ウィンドウ上部のタブをやめ、**左側のメニュー**から機能を選ぶ形に変更しました。全機能が常に一覧できるようになります。
- ウィンドウ幅が狭いときは、メニューが自動でアイコンだけの細幅表示に切り替わります。左上の「≡」ボタンで手動でも切り替えられます。
- メニューの項目に Windows 11 標準のアイコンを付け、選択中の項目には青い縦線が表示されるようにしました。
- 「設定」はメニューの一番下に分けて配置しました。
- 各画面の上部に、今どの画面を開いているかがわかる見出しを追加しました。
- 画面内の切り替え（請求書／領収書など）を、下線付きのすっきりしたデザインに変更しました。
- 文字サイズ・余白・角丸・配色を Windows 11 のデザインガイドラインに合わせて全画面で調整しました。
- 画面のフォントを Windows 11 標準の Yu Gothic UI に変更しました（未搭載の環境では従来の Meiryo UI を使用します）。
- 既定のウィンドウサイズを 1120×760 に広げました。

# CCI請求書・ラベル発行システム v2.2.4

2026年7月31日

## 主な変更

- 請求書PDFの「10％ 税額」「8％ 税額」から不要な空白を削除しました。
- 「合計（税込）」を明細表の直下、税率計算表の上へ移動しました。
- 合計欄を「単位」列から「金額」列までの幅にし、縦罫線を「単価」列の右端へ揃えました。
- 合計欄の罫線を明細表・税率計算表と同じ細さと色へ統一しました。
- 社内専用アプリとして、起動時のログイン画面とパスワード認証を廃止しました。
- 初回はパスワードなしで担当者を選択し、端末ごとに選択内容を記憶するようにしました。
- ファイルメニューから使用中の担当者を変更できるようにしました。
- スタッフ管理から不要になったパスワード変更・リセット機能を削除しました。
- メールの差し込みタグ「会社名」に、対象書類で選択した発行元が反映されるよう修正しました。
- 複数端末から同時発行した場合でも、請求書・領収書番号が重複しない採番方式へ変更しました。
- 発行番号にデータベース上の重複防止制約を追加しました。
- アプリ内アップデートで、対象バージョンの正しいインストーラーだけを選び、GitHubが提供するSHA-256と照合してから実行するよう強化しました。
- SQLiteバックアップを稼働中でも安全に取得できる方式へ変更し、バックアップ作成時と復元前に整合性を検査するようにしました。
- Pillowの将来バージョンで廃止予定の画像処理APIへ対応しました。

## v2.2.1で追加した機能

- まとめて発行のExcel取込で編集した単価・数量を、請求書明細へ反映するようにしました。
- PDF生成に失敗した場合、明細・金額・発行状態を変更前へ戻すようにしました。
- 支払済み請求書の単価・数量が変更されないよう保護しました。
- 銀行口座にデフォルト設定機能を追加しました。
- 請求書PDFの単位表示、振込先表記、行間を調整しました。
- PDFファイル名の名簿NO.を、`NO.` を付けず番号のみで出力するようにしました。
- Microsoft 365の認証トークンをWindowsの暗号化機能で保護しました。
- Microsoft 365の送信アカウント選択、サインアウト、テストメールを追加しました。
- 代理送信、送信間隔、送信中止、連続失敗時の安全停止に対応しました。
- メールテンプレートの個別保存と、差し込みタグのダブルクリック挿入を追加しました。
- 送信前に宛先・CC・BCC・件名・本文を編集できるようにし、修正した件名・本文のテンプレート保存にも対応しました。
- 送信確認画面に請求書PDFプレビュー、差し込みタグ編集、実データを反映した本文プレビュー、最終送信確認を追加しました。
- 送信テンプレートと本文プレビューを見やすい左右2列にし、編集内容をリアルタイム反映するようにしました。
- メールテンプレートを設定から独立したメインタブへ移し、各差し込みタグが何に置き換わるかを一覧で明示しました。
- 送信確認画面の差し込みタグも、選択しやすいスクロール付き2列表へ変更しました。
- 書類種別ごとにメールテンプレートを複数登録し、送信時に選択できるようにしました。
- メールテンプレート管理画面を左右2カラム化し、本文編集欄とスクロール領域を拡大しました。
- テンプレート操作ボタンを2段に分け、狭い画面でも重ならないようにしました。
- 単発発行のメールアドレスを再発行時にも保持するようにしました。
- テストメールの受信先を保存し、再起動後も復元するようにしました。
- メール送信する領収書PDFは、控えを含まない原本のみの単票にしました。

## インストール方法

1. 下記の `CCIBillingLabel_Setup_2.2.4.exe` をダウンロードします。
2. ダウンロードしたファイルをダブルクリックします。
3. 画面の指示に従ってインストールします。

管理者権限は不要です。既存環境へ上書きインストールした場合もデータは保持されます。

Microsoft 365メールを使用していた環境では、旧版の平文認証キャッシュを
削除して暗号化形式へ切り替えるため、更新後の初回のみ再サインインしてください。
